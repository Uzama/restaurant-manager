type MenueItems {
    id: id
    name: String!
    description: String
    price: Int!
}

type Query {
    getMenue(restaurantID:ID!):[MenueItems]
}

input AddMenueItem {
    restaurantID: ID!
    name: String!
    description: String
    price: Int!
}

type Mutation {
    addMenueItem(input: AddMenueItem!): ID!
    deleteMenueItem(id:ID!): ID!
}